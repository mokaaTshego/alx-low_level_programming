#include "main.h"
/**
 *  * add - adds two integers and returns the result
 *  *@a and @b : uses to find sum
 *  * Return: Always 0.
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
